<!-- AppList.xaml -->
<Page
    x:Name="AppListPage"
    x:Class="Windows_App_Lock.AppList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Windows_App_Lock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" x:Name="AppListStack">
        <TextBlock Text="App List" HorizontalAlignment="Center" Padding="30" FontFamily="Segoe UI" FontWeight="SemiBold" FontSize="25"/>
        <TextBlock Margin="35,20,0,0" FontSize="16" Text="The following apps have been locked by the user."/>
        <ListView x:Name="appListBox" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SelectionMode="None" Margin="16">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1" 
                        Padding="10"
                        Margin="2"
                        CornerRadius="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <!-- Minimal space for icon -->
                            <ColumnDefinition Width="*"/>
                            <!-- Maximum space for name -->
                            <ColumnDefinition Width="Auto"/>
                            <!-- Minimal space for delete button -->
                        </Grid.ColumnDefinitions>

                        <Image Source="{Binding IconPath}" Width="30" Height="30" Margin="5" Grid.Column="0"/>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Grid.Column="1" Margin="15,10,0,0"/>
                        <Button Content="Delete" Margin="10,0,0,0" Click="DeleteApp_Click" Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <AppBarButton Label="Add" Icon="Add" Margin="30,10,0,0" Click="AddApp_Click"/>

        <!-- ContentDialog for adding apps -->
        <ContentDialog
            x:Name="addAppDialog"
            Title="Add App"
            PrimaryButtonText="OK"
            PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
            SecondaryButtonText="Cancel"
            PrimaryButtonClick="OKButton_Click"
            SecondaryButtonClick="ClosePopupClicked">

            <StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ListView x:Name="appListView" SelectionMode="Single" Height="300" Width="500">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding IconPath}" Width="30" Height="30" Margin="5"/>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="10,10,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </StackPanel>
        </ContentDialog>
    </StackPanel>
</Page>
